# 冗長性レビューの限定検証（Issue #120）

[Issue #120](https://github.com/thkt/dotagents/issues/120)の初案・改訂案を2026-09-18に確認し、2文削除後の指示を2026-09-19に追加確認しました。既存の評価指示と開発方針を更新し、根拠のある局所的な冗長性を既存の必須指摘・修正へ接続します。Schema、状態遷移、修正上限、公開権限は変更しません。

## 対象と方法

- 実装開始版: `bfeb6c0709b1779def4dcfc1f161885e1ef2e103`。PR #119の修正前 `b24ba51b3ba92728ab75a4c4a5a671d12c817e27`、修正後 `316dae6f340f6b39f65f5a2f4b75d53f0b3620db` を固定し、両方ともPR全体の基準 `a3756eb1012192d9b76bd1e69d977135fda992b4` から評価しました。
- 当時のrunに保存されたIssue #104の全文と、最初の評価の2項目を読み取りました。Issueは当時の評価対象記録のJSONと内容が一致します。初回は過去項目なし、再評価は当時の非default base問題と工程比較の指摘を渡しました。今回の初回試行の結果は再評価へ渡していません。選択報告・共有知識はともに空です。
- 各固定版のソース全体を隔離し、Git履歴は基準版だけにしました。評価者には現在の差分・追加ファイル・要求・check結果・過去項目を渡し、後続履歴、Issue #120、この記録、正解箇所・指摘本文、他試行の応答は渡していません。再評価文脈を使う限定確認であり、旧runの再開や過去評価全体の完全再現ではありません。
- Bun 1.4.2、Codex CLI 0.155.0、`gpt-6-astra` / `high`、read-onlyを使用しました。既存のreviewInstructions・codex-actor・parseReview・processを一時的なホスト処理から利用し、通常のcheckやCIへ検証用モデル呼出しを追加していません。
- 各ソース版でsetupと引数なしのcheckを実行し、修正前は299件、修正後は302件が成功しました。指示の改訂時もソースとcheckの対象hashは同一でした。各モデル実行は正常終了し、実行前後のソースhashと固定版の全追跡ファイルとの一致、8試行のprompt・応答形式・targetId・過去指摘の更新を確認しました。

初案4試行を保存した後、見逃しを受けてヘルパー内の取得まで追う方法と、同じ境界で観測を共用できる条件を具体化し、改訂案4試行を実施しました。各案の4試行は独立したプロセスで並行実行し、互いの応答を共有していません。修正actorは試行に含めず、各試行だけに20分の上限を設定しました。通常実行のモデル時間設定は変更していません。

## 結果と裁定

指摘件数やneeds_changesだけでは判定せず、固定ソースの呼出し経路・同一入力・観測時点と、残す保証を照合しました。

| 指示 | 修正前・初回 | 修正前・再評価 | 修正後・初回 | 修正後・再評価 |
| --- | --- | --- | --- | --- |
| 初案 | 既知3件中1件を検出、2件見逃し | 既知3件中1件を検出、2件見逃し | 解消済み3件の誤指摘なし | 解消済み3件の誤指摘なし |
| 改訂案 | 既知3件すべてを必須指摘として検出 | 既知3件すべてを必須指摘として検出 | 解消済み3件の誤指摘なし | 解消済み3件の誤指摘なし |

- 同一checkoutへの開始条件検査は、初案・改訂案の修正前で検出しました。異なるcheckoutの両方を検査することと、setup後の検査を残す整理案でした。
- correction内のIssue二重取得とpublish内の対象二重取得は、初案では両文脈とも見逃しました。改訂案では、初回の `R1-duplicate-boundary-observations` と、再評価の `R2-duplicate-issue-observation`・`R2-duplicate-target-observation` が該当します。呼出先までの一致と、Issueの生stdout・trim比較・hash、各工程での新しい観測を残す条件が示されました。
- 修正後の既知3件を再び問題扱いする指摘はなく、setup・モデル・commit・push・本文更新を挟む再照合を削る要求もありませんでした。改訂案の修正後評価は、取得済みIssue／targetの局所的な共用と、Setによる同一checkoutの重複排除を確認しています。
- 別の指摘では、verify終了直後からcommit前の同じ照合、setup後等の同じtarget取得、結果表のunconfirmedと保存回数の説明不整合をソースで確認しました。初案ではrun親パスの正規化差、改訂案では失敗時commitのアサーション不足も指摘され、該当する式と呼出し関係を確認しました。これらは今回の3件を再指摘したものではありません。一括した観測統合案の全範囲や実PR上での再現・修正は未検証で、別の改善候補として残します。
- 過去の非default base問題は再評価でfixedになりました。工程比較の項目は、過去のホスト側補足を今回の入力に含めなかったためopenでした。これは根拠不足を保持した判断であり、誤指摘や検出成功へ数えていません。別指摘もあるため全試行の総合判定はneeds_changesでした。

全8試行の入力、対象、check、応答、各項目の裁定、実行記録はホスト側に保持しています。生ログやホスト内パスは公開しません。入力のSHA-256はIssue本文 `319e352d685f71c44d2fd521a14622c4c0208c6aeda05596dd968cff5b53a8b9`、過去評価 `d12d3b4f016b948f8d7e02ac8caaf67ef5daafa39184e1b657928ffc4ddecd3d` です。使用したreview.tsのSHA-256は、初案 `c314856b976eea004ff094dfde853ce71ffd8451dbd2cb5fb8421c96db0f0584`、改訂案 `0c4872ab1ca2adbe0fa95e232c189c9a6048674a1f40e067796bb9dbcfdb643a` です。

この結果は既知の固定事例を用いた変更確認です。最初の失敗を受けて同じ事例で指示を調整しており、未知の事例での独立した検出率、将来の完全な検出、実GitHub更新、速度・費用の改善を保証しません。通常レビューに件数ノルマは設けません。

## 2文削除後の追加確認（2026-09-19）

[PR #121](https://github.com/thkt/dotagents/pull/121)の公開版 `56b7f7af546933c03a6d266517778ebc97e05d33` から、冗長性の段落に重ねていた対象限定検証と人への判断差し戻しの2文を削除しました。両方の責任は既存の共通段落に残ります。使用したreview.tsのSHA-256は `b5836a52bab57491a3e6e28b9fff535807b78e1900a84086cff424aa57c95d17` です。

ホストが公開ソースから新しく用意した同じ修正前・修正後の固定版を、同じ基準版から `gpt-6-astra` / `high`、read-onlyで各2回評価しました。入力は公開[Issue #104](https://github.com/thkt/dotagents/issues/104)と公開ソース、および公開[PR #119](https://github.com/thkt/dotagents/pull/119)・Issue #104から合成した非default baseと工程比較の過去指摘です。初回には過去指摘を渡さず、再評価だけにこの合成指摘を渡しました。非公開の過去評価・旧runの生ログ・会話は使わず、既知3件の正解箇所や他試行の応答も与えていません。これは実際の過去評価の再現ではなく、過去8試行とは再評価入力が異なります。

入力の出典記録・実行用コード・4件のpromptと実行記録・応答を照合しました。4件とも現行の評価指示を含み、prompt hash・targetIdが一致し、正常終了、timeoutなし、実行前後のsource hash一致を確認しました。各固定版の全71追跡ファイルも公開commitと一致しました。ホストが起動済みの4試行をすべて保持し、追加起動や成功するまでの再試行はしていません。

| 指示 | 修正前・初回 | 修正前・再評価 | 修正後・初回 | 修正後・再評価 |
| --- | --- | --- | --- | --- |
| 2文削除後 | 既知3件すべてを必須指摘として検出 | 既知3件すべてを必須指摘として検出 | 解消済み3件の誤指摘なし | 解消済み3件の誤指摘なし |

修正前では、同一checkoutの開始条件検査、correction.readIssue→checkRevisionのIssue二重取得、publish→checkRevisionの対象二重取得について、同一入力・呼出し経路・観測境界と残す保証を指摘に確認しました。修正後ではその3件を再指摘せず、setup・モデル実行・commit・push・本文更新を挟む必要な再照合を削る要求もありませんでした。再評価は合成したIssue参照の項目をfixed、工程比較の根拠不足をopenとし、新規の具体的問題も評価しました。

別の指摘に挙がったdevelopment内の対象取得やcommit直前の照合、準備時のIssue取得、終端verify内外の照合の重なりと、結果表のunconfirmed・保存回数の説明不整合も固定ソースで追跡しました。これらは解消済み3件の再指摘ではありません。統合案の全範囲で保証を維持できるか、実PR上での再現・修正までは検証しておらず、今回の修正範囲には含めません。全4試行の総合判定はneeds_changesでした。既知3件の検出確認と、これら別候補の採否・未知事例の検出率は区別します。過去8試行を含む同じ事例での限定確認であり、今回も将来の完全な検出、実GitHub更新、速度・費用の改善を保証しません。

## 制御の保証と変更量

既存のreview.test.tsで、必須指摘からの修正・再評価、fixedと新規項目の併存、再発、未解決時の公開本文抑止を確認します。これらを削除すると、修正経路の切断や、旧指摘の解消による新規必須項目の取り落としを見逃します。制御処理は変更しないため、同じ保証の模擬ケースやプロンプト全文一致のテストは追加していません。既存テストの削除もなく、失われる検出条件はありません。実モデルの意味判断と制御テストの成功は別の証拠です。

評価指示は5行追加・2行削除（純増3行）、開発方針は2行、検証記録は56行追加です。runtimeの制御処理・テスト定義・設定・スキル・CLIガイドは変更せず、通常フローのノード・矢印・モデル呼出しの増減は各0です。過去8試行と追加4試行は通常フローと分け、コードの純減や運用負担の実測削減とは扱いません。

提出前の全体check、今回の成果物と文書の独立評価、公開した同じheadのCIは通常のホスト処理で確認します。媒体は不要です。既存 #115 の具体的なテスト整理には範囲を広げていません。
