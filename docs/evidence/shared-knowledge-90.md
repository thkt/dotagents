# Issue #90: 共有モデルの接続と確認範囲

対象は[Issue #90](https://github.com/thkt/dotagents/issues/90)の2026-09-17T11:38:02Z版の要求です。比較元は`765adbb29c51747b2d4ada473ca03a3e40ed7651`、比較先は本PRの成果物です。人の採用判断とは別の実装・検証記録です。現在の操作は[CLI手順](../../scripts/README.md#共有知識の選択)、定義は[JSON正本](../knowledge/implementation-start.json)と[生成説明](../knowledge/implementation-start.md)を参照してください。

## 根拠と判断のつながり

| 情報 | 対象版・適用範囲 | 今回の扱い |
| --- | --- | --- |
| Issue #90の要求・合意 | 提示された上記更新版。初回対象はdotagentsの実装開始 | 要求と権限の正本。モデル改訂で変更しない |
| #85・PR #89の採用 | ローカルGitの`7c869f80bd95ea01b47e1ffd35f89e2899657bf2`と開始版のコード・テスト | #90に収録された条件と照合。GitHub APIとWebから原本文を再取得できず、PR本文や当時のCIの再確認とは扱わない |
| `verifyStartInputs` / `verifyReports` | 開始版のdevelopment・research-handoff | 元checkout・開始commit・選択した入力を照合する既存境界として再利用 |
| `config_index` / `report_index` | 開始版のdevelopment.test.ts | 本文を戻してもindex差分があれば停止する検証条件。新しい実障害の報告ではない |
| 準備削減による時間・手戻りの削減 | `less-rework` | 未計測の仮説。採用済み規則や検証成功から改善値を作らない |

追跡例は、`preserve-work`の目的→`start-objects`の本文・index・モード・HEADの区別→`start-identity`の開始判断→#85の採用コード・検証→`index-counterexample`の観測→`revisit-identity`の説明修正案です。モデル内の関係と根拠IDでこのつながりを表します。`revisit-identity`は提案のままで、関係先にあるという理由で選択内容へ本文を追加しません。

新しい結果が前提と反する場合は、既存のfindings/assessmentsに観測、影響するIDとIssue判断、必要な調査・人の判断を記します。本文だけで開始可能とする説明が見つかれば、index条件の結果を示して定義・説明の差分へ戻れます。停止条件そのものを変える案は人へ返します。今回は反例条件と搬送経路を確認し、新しい障害や目的達成の実測を主張しません。

## 置き換えた操作と残る責任

比較の単位は一つの操作・判断・受け渡しです。下表は今回の対象部分だけで、公開・CI・人のマージや撮影など対象外を含む全体数は述べません。

| 主体・操作 | 変更前 | 変更後・矢印の変更理由 |
| --- | --- | --- |
| 人／AI: 定義を更新する | CLIの実装開始節と報告引き継ぎ節で同じ開始条件を手修正 | JSONの該当nodeを修正→生成説明とAI抽出へ。同じ条件の別正本を削除 |
| AI: scopingで根拠を選ぶ | 文書・報告から今回の規則や観測の意味を取り出し、Issueへ説明 | 通常の選択の中で必要ID・blobと今回の要求との関係をIssueへ。固定定義の本文の再記述を省く。意味・適用性の判断は残す |
| 人／AI: 実装へ引き継ぐ | 報告パスと版をCLIへ、意味と条件は担当者が本文から再構成 | 報告CLIは維持。選んだモデルは既存Issue→ホスト抽出→共通入力へ。新しい引数・台帳・承認先は追加しない |
| ホスト: 開始入力を照合する | 設定・必要報告を既存Git検査へ渡す | 選択モデルも同じ入力集合へ。別のGit照合系統を作らない |
| ホスト: setup後を照合する | 元checkoutには開始入力検査、隔離先の報告には本文検査 | 同じ開始入力検査を両checkoutへ適用。隔離先のindex・モード差分の停止も追加。検査時点は減らさない |
| ホスト: AI入力を作る | Issue・報告参照を`researchContext`から各工程へ | 一つの抽出・描画処理から選択済み定義も同じ経路へ。修正指示と独立評価の役割は分離したまま |
| AI: 実装・修正・評価で解釈する | 参照を読み、工程ごとに必要な定義と根拠を整理 | 固定定義の搬送を機械へ移す。AIは開始版と差分、適用範囲、追加根拠、意味・矛盾の評価に集中する |
| AI／人: 結果から前提を見直す | 参照パスと説明から影響を調べる | 既存の評価→nodeの関係→根拠とIssue判断→必要な調査／合意／通常の差分へ戻る。自動改訂・旧評価の流用はしない |

増える処理はIssueブロックの解析、モデルの形式・ID・参照検査、固定blobの選択抽出、説明生成と古さの検出です。JSON・Bun・Gitと既存の参照検査を使い、依存追加や検索・状態管理サービスはありません。コード量は増えますが、開始条件の手書き説明の重複と各工程での固定定義の再組み立てを置き換えます。生成物も増加量に含め、機械へ移した処理を作業全体の消滅とは扱いません。実際の人・AIの所要時間や手戻りは未計測です。

### 開始条件の定義を更新する経路

同じ条件を変更する一回の保守作業について、操作の単位を揃えて比較します。

- 変更前: 条件変更を決める → CLIの開始節を手修正 → CLIの報告節を手修正 → check → 独立評価。
- 変更後: 条件変更を決める → JSONの定義を手修正 → 説明生成を実行 → check → 独立評価。

この経路は双方とも5ノード・4矢印です。定義本文の手修正箇所は2から1へ減り、一つの手修正を生成実行へ置き換えました。生成後のチェック・意味の評価は残ります。AIへの共通入力は同じ定義を機械抽出するため、担当工程ごとの固定本文の転記を必要としませんが、AIの読解や適用判断の削減数は実測していません。全体ノード・矢印の純減や処理時間短縮は主張しません。

## 検証とテスト整理

共通方針に従い、削除した場合に見逃す現実的な不具合を基準に選びました。既存developmentの無関係な追跡差分・未追跡ファイル・モードの保全、HEAD変更、設定・報告の本文とindex変更、setup中の変更を再利用しています。既存correction・reviewの対象同一性と文書版の検証も維持します。既存の開始条件テストは削除していません。選択ブロックの囲みを3本のバッククォートだけに限定した初回実装のテストは、標準Markdownの囲みを受理する検証へ変更しました。囲みの形式制限を要件とはせず、未完・重複・不正参照の拒否を維持しています。

| 追加・更新した検証 | 削除すると見逃す条件 | 負担を抑えた点 |
| --- | --- | --- |
| knowledgeの生成・選択 | 正本改訂が説明に反映されない、古い説明が通る、選択外の提案を入力へ混入、仮説・適用条件・版の欠落 | 一つの変更で生成物とAIの双方を照合。全文snapshotは使わない |
| 説明例と選択の区別 | 外側のコード囲み・引用・リスト・HTML内の例を実選択として取り込む、実選択との併記で不要停止する | 固定Bunの組み込みMarkdown解析を再利用。独自パーサーや依存を追加しない |
| 形式と参照の拒否 | 未知・重複ID、根拠欠落、不正な版・パス、未完の選択ブロック、未確認効果の事実化を受理 | 外部入力境界を小さな同期ケースへ集約。実装の変換で期待値を作らない |
| 3工程の接続 | 初回だけモデルが届く、修正後に開始版を黙って新しい版へ置換、評価対象への保存漏れ、後続改訂に古い成功を流用 | 実Gitと模擬actorの一つのdevelopment→correction→review経路。新しいモデル呼び出しはしない |
| 必要モデルの開始停止 | 選択モデルが報告とは別扱いで本文・index・版・欠落・setup変化を見逃す | 既存の開始fixtureを再利用し、actor起動前の停止を確認 |
| モードとsetup後index | `core.filemode=false`で必要入力のモード変更を無視、隔離先setupがindexだけを変更して開始 | 既存の差分マトリクスに追加。`verifyStartInputs`を再利用 |

対象を絞ったコマンドは、`bun test scripts/tests/knowledge.test.ts scripts/tests/development.test.ts scripts/tests/correction.test.ts scripts/tests/review.test.ts`です。初回は125件成功・1件失敗でした。失敗はモデル未選択時の既存参照出力へ空配列を足した互換性の差で、未選択時は旧出力を保つよう修正しました。修正後の関連範囲と静的検査の結果は以下に記します。

修正後はknowledge・development・reviewの関連94件が成功、失敗0件でした（25.11秒）。correctionの32件は初回に成功しています。変更したTSのOxlint・Oxfmt・Biome、プロジェクトの型検査、`knowledge:check`、`git diff --check`も成功しました。全体checkの代替としては扱いません。スキル標準validatorは環境のPyYAML欠落で起動できなかったため、BunのYAML parserで必須metadata・名前・description・未完placeholderと、変更スキルの相対リンクを別途確認しました。

制御テストのactor・GitHub応答は模擬です。入力・出力とGit差分の搬送は確認できますが、実モデルによる3工程全体の意味判断・操作時間・効果の測定とは区別します。独立レビューも、この実装差分・文書・テストに対する評価であり、実運用の成果ではありません。必要媒体を伴うUI変更はなくcapture定義は追加していません。

## 差分量の比較

行を同じ単位として、開始commitから今回の差分を集計します。未追跡の新規ファイルは全行を追加として含めます。生成物や構造化正本を除外して純減とは扱いません。この数値は効果・保守性・工程数の指標ではありません。

| 区分 | 追加行 | 削除行 | 差引 | 範囲 |
| --- | ---: | ---: | ---: | --- |
| 実装コード | 313 | 17 | +296 | scripts/{knowledge,development,correction,research-handoff,review}.ts |
| テスト | 358 | 1 | +357 | scripts/tests/{knowledge,development}.test.ts |
| 手書きの現行文書 | 32 | 9 | +23 | 開発方針、README、CLI手順、scoping参照、implementスキル |
| 構造化した知識正本 | 274 | 0 | +274 | docs/knowledge/implementation-start.json |
| 生成した説明 | 153 | 0 | +153 | docs/knowledge/implementation-start.md |
| 手書きの検証記録 | 92 | 0 | +92 | この文書 |
| 実行設定 | 4 | 2 | +2 | package.json（生成物照合の追加。既存checkは維持） |

開発方針の追記2行（段落1行と空行）も上のcheckout差分へ含めています。対象のノードと矢印の変更理由は操作比較表に示し、関数移動や図の箱の集約を削減量にしていません。

## 続行と最終確認

初回の全体checkは250件成功・失敗0件でした。独立評価中に実行上限へ達し、説明用コード例の誤読という途中指摘を残して停止しました。依頼者の「引き続き進めて」に基づき、旧状態・消費量を保全して修正と評価を続けます。通常の実行上限や停止規則を変更する機能は本PRに含めません。

誤読は[Bunの組み込みMarkdown解析](https://bun.com/docs/runtime/markdown)を使って修正しました。引用・リスト・HTMLと外側のコード囲みの内容は選択せず、有効なブロックだけを抽出します。閉じていない選択は受理しません。形式確認の成功を内容・合意の保証としない原則は変わりません。

最終の全体check・独立評価と同一headのCI結果はPRに記載します。この文書を書いた時点では修正版の最終評価・公開は未完了です。開発方針、スキル、モデル・生成説明と本記録も最終評価の対象に含め、人がレビューとマージを判断します。

### R1: Markdown表を含むIssueの選択解析

評価対象`cf12f887dde02488d92aa5f802c3abe3637fcf2f36d6d6319530aa7b1edec2f6`への必須指摘`R1-markdown-table-selection`をBun 1.4.2で再現しました。表のセル文字列が選択JSONへ混入する原因は、抽出時に表の描画を除外していないことでした。[選択解析](../../scripts/knowledge.ts)で表も除外し、[既存テスト](../../scripts/tests/knowledge.test.ts)の通常本文を、モデル未選択では表を含む本文、選択ありではブロックの前後に表を置く本文へ拡張しました。この検証を削除すると、既存Issueの開始停止と有効選択の併記失敗を見逃します。同期の既存ケースへ統合して実行負担を抑え、従来の未選択・JSON形式のIssue入力・不正選択の検出条件は維持しています。削除した検出条件はありません。

修正前に更新テストが同じJSON解析例外で失敗し、修正後は`bun test scripts/tests/knowledge.test.ts`の5件が成功しました。ホストの評価対象記録にあるIssue #90本文も直接解析し、未選択の空配列を返すことを確認しました。変更したTS 2ファイルのOxfmt・Oxlint・Biomeも成功しています。現在のCLI説明・選択形式・権限は変更不要で、既存の説明どおりに動くための修正です。全体checkと修正後の独立評価はホストへ引き継ぎます。模擬actorの成功を実モデルの意味判断や公開の証拠とは扱いません。
