# tantekiの限定評価（2026-09-24）

状態: 採用判断のための限定実験。Skill・lint設定・共有フローは変更していない。

## 問いと事前基準

現行の文書作成方針にtantekiを足したとき、必要な意味や正本参照を保ったまま、読みやすさ・不足発見が改善するか。その増分は実行負担に見合うか。事前に固定した基準は、事実・数値・否定・条件・権限、未決定と提案の区別、読者が判断または操作できること、正本リンク、重複・冗長さ、lintの有益性、時間・CLI使用量である。安全・内容の欠陥を短さより重く見る。各課題の必須項目は末尾の原資料に対応し、実行前に別の評価メモへ列挙した。

## 実行条件

- 対象は合成したADRとRunbook。各課題を現行方針のみ／同じ方針+tantekiで1回ずつ、新規ephemeral Codexセッションにより作成した。使用モデルは`gpt-6-sol`、reasoning medium、CLI 0.156.1。課題・基準・資料は実行前に固定し、評価基準そのものを作成モデルへ渡していない。
- 共通方針はdotagentsの公開HEAD `16d70c23918d69d4dbc01795ac1a886938ee573d` の`.codex/DEVELOPMENT.md`から日本語確認節を抽出した。候補はtanteki commit `cf51a7bab450253a4e1fc75cdf35692111b4bddb` のSkill、参照資料、lint実装。依頼文で候補だけSkill利用を指定した。
- macOSのOS隔離で元のユーザー領域と未commit Skillの読取りを拒否し、公開資料・合成資料と認証ファイルだけを使わせた。元Skillの読取り拒否を起動前に検証した。Git操作・公開はしていない。4実行のコマンド記録に元ユーザー領域を読むコマンドはなく、認証用symlinkは終了後に除去した。
- 原資料と成果物は末尾に全文を保存する。生のCLIイベント、lint JSON、プロンプト、事前基準の元ファイルはローカル一時領域`/private/tmp/tanteki-trial-20260924/`にあり、永続保存はしていない。モデル入力トークンにはcache分を含む。請求額は未測定。

## 結果

| 課題 | 意味・参照の判定 | 文字数（基準→候補） | 経過秒（基準→候補） | 入力token（基準→候補） | 実行コマンド数（基準→候補） |
| --- | --- | ---: | ---: | ---: | ---: |
| ADR | 両方とも必要な決定・不採用理由・未決定事項・CI正本リンクを保持。候補だけの明確な意味改善は確認できない | 708→667 | 66→131 | 103,162→402,542 | 4→16 |
| Runbook | 両方とも操作・停止・未決定ゆえ公開不可を保持。候補は監視画面の5分間隔と3分以内の確認との関係が資料だけでは分からないことを明示。基準はこの不足を指摘しなかった | 1,020→947 | 59→106 | 104,201→216,263 | 4→13 |

候補の最終成果物は両方ともtantekiのlint終了コード0だった。基準成果物にも事後に同じlintをかけると両方0で、生成された2文書ではlintの差は観測されなかった。ADR候補では最初の`npm ci`が隔離環境の書込み先で失敗し、依存を再導入してからlintが通った。この再試行がADR候補の時間と使用量を増やしているため、その数値を定常運用の比較として扱わない。Runbook候補は依存導入後の実行だが、それでも基準より経過47秒、入力112,062 token、実行コマンド9件が多かった。出力tokenはADRが2,181→3,632、Runbookが2,428→3,801。使用量と経過時間はモデルのばらつきとキャッシュ状態にも左右される。

Runbook候補の不足指摘は有益だった。原資料は「成功後3分以内に確認」を求め、監視資料は「5分間隔のグラフ」と記す。これだけで3分以内に新しい値を確認できるかは分からない。候補は実行可能な手順と断定せず、表示の扱いが未提示と残した。ただし、これは1回だけの観測であり、Skillが安定して同種の矛盾を見つける保証ではない。

## 既存文書へのlintのみの試行

同じlintを公開HEADの`README.md`と`.codex/DEVELOPMENT.md`に`--type stock`で実行した。READMEは5件（文長1、表セル2、stock-boundary 2）、開発方針は27件（stock-boundary 6、助詞反復3、no-ai-jargon 10、no-vague-action 4、表セル2、max-ten 1、文長1）だった。長い表セルや文は読み直す候補になる。一方、stock-boundaryの8件は採用判断の根拠として必要なIssueリンクを追跡情報と誤認する。`必要に応じて`の4件は判断条件に幅を残す文脈でも警告され、`観測`などハーネスで意味のある語もno-ai-jargonの対象になる。これらを自動修正・必須ゲートにすると根拠や条件を失うおそれがある。個別の真の欠陥数は確定していない。

## 判断と限界

現時点ではtanteki全体の常用・ハーネス組込みを推奨しない。現行方針だけでも両課題の必須情報は保たれ、候補は一つの重要な不足を見つけたが、2課題各1回では安定した増分を示せない。もし導入を検討するなら、Runbookの時間条件と観測手段の整合を独立の評価観点に加え、既存の日本語lint検討では表セル・文長等を助言として試す。正本へのIssueリンクを誤検出するstock-boundaryと、文脈依存の語彙規則をそのまま必須化しない。長い実文書、実際の改稿、費用と再現性は未評価である。

## 入力と成果物

以下は合成原資料と4つの成果物の全文。原資料には公開版方針を別途共通で与えた。SHA-256は各ファイルの先頭16桁である。

実行前に固定した課題別の必須条件は次のとおり。ADRでは6サービス、2026-08-03の2件と本番障害の記録なし、同じSHAの必要なCI、head変更時の旧判定破棄と本文・媒体・CIの再確認、人のマージ承認、二つの不採用理由、readyだけの範囲、四つの未決定事項、CI要件リンクを確認する。Runbookでは対象と閾値、デプロイ確認、二つの正確なコマンド、失敗時の停止、3分以内の流入・キュー確認、戻す条件、10分以内の再確認、二つの未決定事項と公開不可、監視リンクを確認する。必須情報を保ち、余計な断定がなければ意味合格とし、未決定の手順を実行可能と扱うことやlintに合わせて必要情報を削ることは重大欠陥とした。

### ADR原資料

SHA-256: `0443e86c6136a24d`

```md
# 原資料: ready 判定を commit に固定する

製品は6サービスを扱うリリース補助ツール。2026-08-03に、PRのheadが変わった後、変更前のCI結果を使ってreadyと表示した事例が2件あった。これらで本番障害が起きた記録はない。

採用済みの決定: ready判定はPR番号だけでなく、その時点のcommit SHAとCI結果の対象SHAを記録する。同一SHAの必要なCIが成功している場合にだけreadyへ進める。判定中にheadが変わったら、旧判定を破棄し、新SHAの本文・必要媒体・CIを再確認する。マージは引き続き人が承認する。

不採用案: ブランチ名と最新CIを参照する方式はhead変更時に対応を取り違えるので採らない。判定中ブランチをロックする方式は並行作業を妨げるので採らない。

範囲: readyへの切替判定だけ。CI providerの移行、リトライ間隔、マージ権限は決めていない。スクリプトの細部も未決定。

正式なCI要件の一覧は [CI要件](ci-requirements.md) が正本。本文は必ずその参照先を残す。
```

### ADRのCI正本

SHA-256: `db9d0e6d3708200e`

```md
# CI要件

ready判定ではunitとintegrationを確認する。両方とも同じcommit SHAが必要。
```

### Runbook原資料

SHA-256: `48641b9a3c212147`

```md
# 原資料: reporter の取り込み一時停止

この文書は当番担当者が操作中に使う手順書。対象は reporter の us-east-1 のみ。イベント取り込み数が5分連続で毎分1,000件を超えた時に検討する。まず対象デプロイIDとリージョンを確認し、デプロイ進行中なら操作しない。停止フラグのコマンドは `opsctl flag set reporter REPORTER_PAUSE_INGEST=1 --region us-east-1`。コマンドが失敗したら再実行を繰り返さず、操作を止める。

成功後3分以内に監視画面で流入数の減少とキュー増加の有無を確認する。流入数が減らない、またはキューの件数が増え続ける場合は停止フラグを戻す。戻すコマンドは `opsctl flag set reporter REPORTER_PAUSE_INGEST=0 --region us-east-1`。復旧後10分以内に流入とキューを再確認する。

最大停止時間と、当番が不在の場合の連絡先は未決定。操作前にこの2点を確定できなければ実行可能な手順書として公開しない。監視画面の正本は [reporter監視](monitoring.md)。省略せず本文中からリンクする。
```

### Runbookの監視正本

SHA-256: `3c9d086d3c053978`

```md
# reporter監視

流入数とキュー件数を5分間隔のグラフで確認する。
```

### ADR基準

SHA-256: `6282d28cce43a2ee`

```md
# ADR: ready 判定を commit SHA に固定する

## 状態

採用済み

## 背景

このリリース補助ツールは6サービスを扱う。2026-08-03に、PRのheadが変わった後、変更前のCI結果を使ってreadyと表示した事例が2件あった。これらの事例で本番障害が起きた記録はない。

ready判定では、確認したCI結果が判定対象の変更に対応している必要がある。ブランチ名やPR番号だけでは、判定中にheadが変わった場合の対応関係を特定できない。

## 決定

ready判定では、PR番号に加えて、その時点のcommit SHAとCI結果の対象SHAを記録する。[CI要件](ci-requirements.md)に定めるunitとintegrationの両方が、同一SHAで成功している場合にだけreadyへ進める。

判定中にPRのheadが変わった場合は旧判定を破棄し、新しいSHAについて本文・必要媒体・CIを再確認する。readyへの切替は承認を意味せず、マージは引き続き人が承認する。

## 理由と不採用案

commit SHAを判定に結び付けることで、head変更前のCI結果を変更後のready判定に使うことを防ぐ。

- ブランチ名と最新CIを参照する方式は、head変更時に変更とCI結果の対応を取り違えるため採らない。
- 判定中にブランチをロックする方式は、並行作業を妨げるため採らない。

## 範囲と未決定事項

この決定の範囲はreadyへの切替判定に限る。CI providerの移行、リトライ間隔、マージ権限、スクリプトの細部は決めていない。
```

### ADR+tanteki

SHA-256: `d6b8f7b17cf02c29`

```md
# ADR: ready 判定を commit SHA に固定する

状態: 採用済み

## 背景

このリリース補助ツールは6サービスを扱う。2026-08-03に、PRのheadが変わった後、変更前のCI結果を使ってreadyと表示した事例が2件あった。これらによる本番障害の記録はない。readyへの切替には、確認した内容とCI結果が同じheadに対応している必要がある。

## 決定

ready判定では、PR番号に加えて判定時のcommit SHAとCI結果の対象SHAを記録する。[CI要件](ci-requirements.md)に定める必要なCIが同一SHAで成功し、PR本文と必要な媒体の確認が済んだ場合にだけreadyへ進める。判定中にheadが変わった場合は旧判定を破棄し、新しいSHAの本文・必要な媒体・CIを再確認する。

この決定はreadyへの切替判定に限る。マージは引き続き人が承認する。

## 決定理由と影響

CI結果の対象SHAを判定時のcommit SHAに対応付けることで、変更前のCI成功を新しいheadのready判定に流用しない。ブランチ名と最新CIを参照する案は、head変更時に対応を取り違えるため採らない。判定中にブランチをロックする案は、並行作業を妨げるため採らない。

headが判定中に変われば、本文・必要な媒体・CIの確認を新しいSHAについてやり直す必要がある。CI providerの移行、リトライ間隔、マージ権限、スクリプトの細部はこの決定では定めていない。
```

### Runbook基準

SHA-256: `c1a29ae92d0a0baa`

```md
# reporter 取り込み一時停止 Runbook

> **状態: 実行不可（実行可能な手順書として公開不可）。** 最大停止時間と、当番が不在の場合の連絡先が未決定。操作前に両方を確定できなければ、この文書を実行可能な手順書として公開せず、停止フラグを操作しない。

## 対象と検討条件

- 対象は reporter の **us-east-1 のみ**。
- イベント取り込み数が **5分連続で毎分1,000件を超えた時**に、一時停止を検討する。この条件だけで操作を決定しない。
- 流入数とキュー件数は[reporter監視](monitoring.md)で確認する。正本に記載されたグラフの間隔は5分。

## 操作前の確認

1. 最大停止時間と、当番が不在の場合の連絡先が確定していることを確認する。いずれかを確定できなければ操作しない。
2. 対象デプロイIDとリージョンを確認する。リージョンが us-east-1 でない場合、この手順は対象外。
3. デプロイの進行状況を確認する。**デプロイ進行中なら操作しない。**

## 一時停止と停止判断

1. 上記の確認後、次のコマンドで停止フラグを設定する。

   ```sh
   opsctl flag set reporter REPORTER_PAUSE_INGEST=1 --region us-east-1
   ```

2. コマンドが失敗したら、再実行を繰り返さず、操作を止める。
3. 成功後**3分以内**に[reporter監視](monitoring.md)で流入数の減少とキュー増加の有無を確認する。
4. **流入数が減らない、またはキュー件数が増え続ける場合**は、次のコマンドで停止フラグを戻す。

   ```sh
   opsctl flag set reporter REPORTER_PAUSE_INGEST=0 --region us-east-1
   ```

5. 復旧後**10分以内**に、[reporter監視](monitoring.md)で流入数とキュー件数を再確認する。

## 未決定事項

| 項目 | 状態 |
| --- | --- |
| 最大停止時間 | 未決定 |
| 当番が不在の場合の連絡先 | 未決定 |

この2点が確定するまで、本書を実行可能な手順書として公開しない。
```

### Runbook+tanteki

SHA-256: `8f9b254604331238`

```md
# reporter の取り込み一時停止（草稿）

当番担当者が、`us-east-1` の reporter のイベント取り込みを一時停止する際の手順。**最大停止時間と当番不在時の連絡先は未決定。操作前に両方を確定できなければ、実行可能な手順書として公開しない。**

## 実行前の確認

1. イベント取り込み数が**5分連続で毎分1,000件を超えた**ことを確認する。この条件を満たしたときに一時停止を検討する。
2. 対象のデプロイIDとリージョンを確認する。対象は `us-east-1` の reporter のみ。デプロイが進行中なら操作しない。
3. 最大停止時間と当番不在時の連絡先が確定していることを確認する。未確定なら操作に進まない。

流入数とキュー件数は、正本の[reporter監視](monitoring.md)で確認する。監視画面には5分間隔のグラフがある。3分以内の確認に必要な表示の扱いは、原資料に示されていない。

## 一時停止と停止判断

1. 次のコマンドで取り込みの停止フラグを設定する。

   ```sh
   opsctl flag set reporter REPORTER_PAUSE_INGEST=1 --region us-east-1
   ```

   コマンドが失敗したら、再実行を繰り返さず操作を止める。

2. コマンドが成功したら、**3分以内**に[reporter監視](monitoring.md)で流入数が減ったか、キューの件数が増えているかを確認する。

3. 流入数が減らない、またはキューの件数が増え続ける場合は、次のコマンドで停止フラグを戻す。

   ```sh
   opsctl flag set reporter REPORTER_PAUSE_INGEST=0 --region us-east-1
   ```

## 復旧後の確認

復旧後**10分以内**に、[reporter監視](monitoring.md)で流入数とキュー件数を再確認する。

## 未決定事項

- 最大停止時間
- 当番不在時の連絡先

この2点が確定するまで、本草稿を実行可能な手順書として公開しない。
```
