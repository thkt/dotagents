## 目的と根拠

#106で編集元を確定したローカルHTMLについて、現行コード・方針と判断経路の対応を揃え、同じ事実を複数箇所へ手作業で反映する負担を減らす。優先度は中。今回の再調査で見つかった図の3指摘を、同じ編集元と画面確認を使う本Issueへ集約する。

- `docs/workflow-map.html` の概要描画用 `pOrder` は、既存の `flow-data.phases` と同じ14フェーズの順序を別定義している。
- `docs/workflow-guide.html` は同じ参照commitを本文ラベルと根拠リンクへ反復している。#106の画面確認では短縮commit表記の更新漏れが見つかり、修正された。

### 判断経路の再調査で確認した点

- X04はneeds_humanを選んでからX05の応答検査へ進む図だが、[実装](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/correction.ts#L656)は応答検査後に有効なstatusから行き先を選ぶ。
- Z12・Z13は停止後の事実照合と次の作業の判断を共に人の担当としている。[既存方針](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/skills/implement/SKILL.md#L12)では、調査できる事実は調べ、人にしか決められないことを判断材料とともに返す。
- S23から人のS25を必ず通る表示は、既存の有効な合意を再利用する扱いが読み取りにくい。[scoping](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/skills/scoping/SKILL.md#L20)は合意済み事項を聞き直さない。実際に不要な再質問が発生したという観測ではなく、図の曖昧さである。

## 範囲と受入条件

- [ ] X04/X05は、有効な応答を検査した後に通過・人の判断待ち・不正応答による停止を選ぶ表現へ揃える。既存の応答検査と分岐を一つの説明へまとめる案を優先し、無検査のneeds_humanを人へ渡すように描かない。
- [ ] Z12/Z13は進行の停止と対応する担当を分けて表す。既存の許可範囲で確認できる事実は担当AI・ホストの調査へ、要求・権限・上限等の判断は人へ渡す。環境調査の許可を環境変更の許可へ拡大しない。
- [ ] S23/S25は既存の有効な合意を再利用する場合と、新しい人の判断を待つ場合を区別する。回答後・支援後は元の判断に必要な根拠と条件を再確認し、完了だけを通過としない。
- [ ] 「通す／人が確認する／止める／支援につなぐ」は行き先を整理する観点として使う。runtimeの新機能、全ノード共通の4分岐、新しい状態機械、旧runの自動再開は描かない。現行CLIの停止と新しい作業の開始を区別する。
- [ ] 図のフェーズ順は既存のphasesを正本とし、描画側の同じ順序の列挙をなくす。概要・ナビゲーション・詳細の順序と内容を維持する。
- [ ] ガイドの現在の参照版を一か所で更新できる最小案を採る。現在版のラベルと根拠リンクを揃え、過去版を意図した参照まで一括置換しない。
- [ ] 各HTML内の既存データ・描画処理を使い、共通metadataファイル、動的GitHub取得、ビルド工程、全文生成器、ライブラリを追加しない。HTML・スクリプトの増減を含め、減る更新箇所と保守負担を比較する。
- [ ] 着手時の全ノード・全矢印（調査時は205ノード・333矢印）が表す必要な条件・責任・根拠・停止理由を保持する。誤った接続は訂正し、統合する場合も元の情報と既存IDの参照先を辿れるようにする。ID検索と直接リンクを壊さず、情報を省くだけの削減としない。
- [ ] 現行ガイドと図だけをローカルで変更し、旧下書き・他の未追跡資産を保全する。HTML、画面証拠、生ログを公開repoへ追加しない。

## 検証とローカル引き継ぎ

編集前に現行2ファイルを今回のcheckout外作業領域へ保全する。ID・リンク・データ接続と版表記を確認し、通常幅と390px幅で概要・検索・選択・停止表示・根拠リンクを実画面確認する。画面証拠は同じローカル作業領域へ保存する。変更内容と日本語は原資料および既存の独立評価で確認する。

#106完了時の編集元は以下。着手時にhashと差分を確認し、不一致なら最新内容を読んで対象を再確定する。Git HEADだけで未追跡HTMLを取得できるとは扱わない。別環境では合意した方法で現行ファイルを引き継ぐ。

| ファイル | SHA-256 |
| --- | --- |
| `docs/workflow-map.html` | `c2c3d62adc6dcbc27c4ed94c56bd25485770d727c5c595f037119b7c642eed9e` |
| `docs/workflow-guide.html` | `d0a511c1fd6e70603248d22372fd9c2a8999599ff12d070e5d93d8b7af221a66` |
| 保全する `docs/workflow-guide-draft.html` | `8fdb45610abbfd9500b049db815f7a0e567f2f228ea72663c6ec8f1a393b05c4` |

runtimeと共通テストは変更しない。図のノード・矢印は、訂正・責務の統合を含む変更前後で数え、実処理の削減とは分ける。X04/X05の統合は局所的には1ノード・1矢印減の見込みだが、既存IDへの参照維持と他の訂正を含む総数は実装後に確認する。合意再利用の経路を明示して矢印が増える場合も、不要な人待ちを避ける効果と区別して記す。HTML内のコードと更新箇所の増減も本Issueに記す。既に終了したガイドと旧下書きの二重更新を、新たな削減として数えない。提出前は既存環境の `bun run check` を行うが、HTMLの画面検証とは別の証拠として扱う。依存導入が必要なら `bun install --frozen-lockfile --ignore-scripts` を使う。

確認日: 2026-09-19。参照コード版: `0fec56c536763f8ccbd4bc4a9080c19beb14862d`。依頼者の整理候補10件のIssue化依頼で、同じローカル資料を扱う2件をまとめた。その後、4分類での再調査を受けた「issue整理しましょう」の指示に基づき、上記の判断経路・担当の3点を追加した。今回はIssue公開までとし、編集は後続の着手指示で進める。本文と #106 に必要な方針・根拠を残し、別の必須報告は作らない。本Issue自体の別評価者による独立評価は未実施。

## 他Issueとの境界

#127はIssue公開後の確認統合、#128はPR本文の確認責任、#134はレビューから人へ直接戻す契約の調査。本Issueで未採用の方針変更やruntimeの経路を先に描かない。#132・#133を含む関連変更が採用済みなら、その版を根拠に表示を合わせる。必要な手順・検査と停止理由を削って件数だけを減らさない。
