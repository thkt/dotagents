## 位置付けと関連（2026-09-19整理）

優先度: 中。#132・#133の局所修正に必要な検証を先に既存fixtureで成立させ、その採用後に重複準備を整理する。

通過、判断待ち、停止、支援後の再判定について、異なる根拠・行き先・後続抑止の保証を統合で失わない。4分類ごとの専用台帳や全ノードの網羅マトリクスを追加せず、対象の現実的な失敗に絞る。

## 目的と根拠

[development.test.ts](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/tests/development.test.ts#L604)は49種類のmodeを一つのループで扱い、準備、Git/GitHub応答、変更注入、結果確認にmode分岐が分散している。ケース追加・修正で追う箇所を減らす。49件すべてが重複しているという判断ではなく、共通fixtureと個別条件の配置が対象。

#115の価値判断方針を使い、そのIssueで済んだレビュー入力テスト等の整理を繰り返さない。

## 範囲と受入条件

- [ ] 固定のrepo準備、外部応答、後片付けだけを必要な範囲で共有し、各ケースの変更入力・失敗注入・期待する停止理由を近くで読めるようにする。
- [ ] ケースごとの状態と後片付けを独立させ、不要なmode分岐と重複準備を取り除く。別ファイルへの移動だけで完了としない。
- [ ] 対象・権限変更、公開結果不明、保存失敗、割込み等の異なる保証を維持する。対象外の条件を正常にして、意図した理由で停止し、必要な証拠と副作用抑止を確認する。
- [ ] テストを削除・統合する場合は、失う検出条件と残る検証を説明する。件数維持や全件削減を目標にしない。
- [ ] 新しいシナリオ言語・汎用テストエンジン・設定層を作らず、無関係なテスト全体へ整理を広げない。

変更前後で同じ条件のテストを実行し、コード・準備・分岐の保守負担と時間を比較する。検出力が不明な箇所だけ範囲を絞った不具合注入を使う。runtimeの手順・図のノード・矢印は変更しない。テスト行数や時間の純減量は未測定。

## 検証・削減評価・引き継ぎ

セットアップは `bun install --frozen-lockfile --ignore-scripts`。変更の提出前は `bun run check` と差分の独立評価を行い、PRでは同じheadの `checks`・`verify` を確認する。既存の関連テストを使い、不足する現実的な失敗条件だけ追加・更新する。プロダクト画面媒体は不要。

実装・テスト・文書の増減と、同じ範囲・粒度でのノード・矢印の増減を既存のPR説明に記す。実際の処理・操作の削減と表示上の集約を区別し、純減がなければその理由を示す。行の圧縮、単なる移動、新しい計測基盤・必須帳票は目的にしない。変更を挟む再照合、停止時の保全、独立評価、人の要求判断とマージを維持する。

確認日: 2026-09-19。調査版: `0fec56c536763f8ccbd4bc4a9080c19beb14862d`。依頼者が整理候補の提示を受けて「issue化してください」と指示した範囲。今回はIssue公開までとし、実装は後続の着手指示で進める。着手時に最新の採用版へ照合し直す。必要な根拠は本文と固定版の参照で足り、未共有の下書きや生ログを必須入力にしない。本Issue自体の別評価者による独立評価は未実施。
