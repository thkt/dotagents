## 計画変更により実施しない

2026-09-17、依頼者は運用とコードの両方を簡素化する計画への変更を指示した。[#84](https://github.com/thkt/dotagents/issues/84)で専用校正自体を廃止するため、本Issueの無変更時の最適化は実装しない。`not planned`として閉じる。実装済み・検証済みという意味ではない。

以下は経緯を残すための旧計画であり、現在の実装要求ではない。

---

## 目的と現状

内容が変わらない校正結果に対する追加のAI確認とファイル置換を省き、待ち時間と不要な副作用を減らす。

現行の `reviewWriting` は候補が原文と完全一致してもCodexの意味照合を実行し、documentsモードは候補の全ファイルを置換する。原文と同じ模擬応答でも後続の呼び出しが行われることを確認した。実モデルでの発生頻度と削減時間は未計測。

## 範囲と完了条件

- [ ] 候補の形式、文書名、重複・欠落を検証した上で、対応する本文が全件完全一致する場合は意味照合モデルを起動しない。空白や改行の差を丸めて一致扱いしない。
- [ ] 完全一致による機械判定と、モデルによる意味照合を記録と説明で区別する。実施していないモデル確認の記録を作らない。一部でも異なる場合は、現在の意味照合を行う。
- [ ] documentsモードは内容が変わるファイルだけ置換する。fileモードで新しいoutputを渡す契約は維持する。
- [ ] 完全一致でも完了前の本文・根拠・対象選択の再照合を行う。変更・欠落・不正候補・中断を成功へ読み替えない。同一実行内の再利用と利用不能記録の扱いを維持する。
- [ ] 開発方針とCLI説明を、完全一致時の機械判定と変更時の意味照合に合わせる。文書ごとの細かなキャッシュ、モデル変更、校正の任意化は含めない。

## 重点的に確認する振る舞い

`writing.test.ts` と `writing-review.test.ts` を使い、完全一致時の不要な呼び出し・置換の抑止、差分ありの意味照合、対象や根拠が処理中に変わった場合の採用拒否を確認する。省いた処理と保持した確認を、返却結果・記録・ファイルへの副作用から確かめる。

## 既存Issueとの分担

[#59](https://github.com/thkt/dotagents/issues/59)の対象選択と、[#71](https://github.com/thkt/dotagents/issues/71)の同一実行内の再利用を前提にする。本Issueは、新しく校正した候補が原文と同じ場合の処理を省く追加変更。意味照合を常に別モデルで行う現行方針は、完全一致の場合に限って更新する。

## 実施順序

簡素化計画のP1。最初に実装する単位。調査時の公開済みmainは上記の調査版と一致する。

## 検証と引き継ぎ

対象repoのセットアップは `bun install --frozen-lockfile --ignore-scripts`、提出前の検証は引数なしの `bun run check`。既存の検証を再利用し、不足する現実的な失敗条件だけを追加・更新する。削除する検出条件は理由と残る検証を示す。独立評価と、同じPR headの `checks`・`verify` を確認し、人がレビューとマージを判断する。

プロダクト画面媒体は不要。必要な共有報告は `research/`、内部の確認記録は既存CLIのcheckout外保存先へ置く。模擬コマンドの制御テスト、実モデル、実際の公開は別の証拠として扱う。

要求と合意、対象版・公開権限の照合、独立評価、人の承認を維持する。旧run・失敗記録・他作業の差分を削除・変換せず、予算の変更や自動再開を含めない。新しい実行エンジンや承認工程は作らない。

## 根拠と合意

確認日: 2026-09-17。調査版: `0b3c61115337c15f14db41ea8f3fee5291497e45`。

- [scripts/writing.ts](https://github.com/thkt/dotagents/blob/0b3c61115337c15f14db41ea8f3fee5291497e45/scripts/writing.ts)
- [scripts/writing-review.ts](https://github.com/thkt/dotagents/blob/0b3c61115337c15f14db41ea8f3fee5291497e45/scripts/writing-review.ts)
- [.codex/DEVELOPMENT.md](https://github.com/thkt/dotagents/blob/0b3c61115337c15f14db41ea8f3fee5291497e45/.codex/DEVELOPMENT.md)
- [scripts/README.md](https://github.com/thkt/dotagents/blob/0b3c61115337c15f14db41ea8f3fee5291497e45/scripts/README.md)

依頼者はリーン生産方式を参考にした簡素化を依頼し、P1〜P6の修正計画を提示した後、2026-09-17にscopingでIssue化するよう指示した。本Issueはそのうちの範囲を定める。今回の作業はIssue公開までで、実装・マージは未実施。改善時間や品質への効果は未計測である。

今回必要な根拠と条件は本Issueと上記のコミット済み参照に含めたため、追加の必須調査報告はない。ローカルの計画・Issue下書きは未コミットであり、実装入力の前提にしない。実装または調査の開始時には、先行変更を含む採用済みHEADで参照の適用性を再確認する。調査版を後続作業の開始commitへ固定しない。
