## 位置付けと関連（2026-09-19整理）

優先度: 中。#123・#124・#132で同じ公開前後の実装を触るため、採用後の差分へ照合する。

本Issueは内部の受け渡しの整理であり、通過条件、停止理由、次の担当を決める規則は変更しない。引数の変換を外しても、外部状態と権限の再照合を省かない。

## 目的と根拠

[development](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/development.ts#L586)は同じプロセス内の公開関数へCLI引数の配列を作って渡し、[publish](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/publish.ts#L44)が解析し直す。既存PR修正では既に保持する情報を得るため、verification設定ファイル全体も読み直して検査する。

CLI境界と内部処理の責務を揃え、型付きの入力を渡せる最小構成へ整理する。追加する型・接続処理を含むコード純減は未測定で、汎用化自体を目的にしない。

## 範囲と受入条件

- [ ] 内部呼出しは必要な値を型付き入力で渡し、CLI引数の組立てと再解析をなくす。standalone publishの既存CLIと必要な入力拒否を維持する。
- [ ] standaloneの外部ファイル・引数は引き続き検査し、内部入力との接続でも公開対象の整合を失わない。型があることを外部状態の保証にしない。
- [ ] 内部経路で不要になる設定の再読取りは整理するが、最新のrepo・主体・権限・PR本文・head・base・Issueの再照合と公開後確認は維持する。
- [ ] 新規PR・既存PR更新の公開副作用、失敗・割込み・結果不明の扱いを変えず、新しい公開モードや汎用実行層を作らない。
- [ ] 接続処理を含め、最終的に残る定義・分岐・受け渡しを比較する。複雑さが増すだけなら広い共通化を採用せず、具体的な削減効果が得られない場合は判断を戻す。

主な検証は `publish.test.ts`・`revision.test.ts`・`development.test.ts`。公開前後の安全境界を残すため、図のノード・矢印削減は現時点で見込まない。

## 検証・削減評価・引き継ぎ

セットアップは `bun install --frozen-lockfile --ignore-scripts`。変更の提出前は `bun run check` と差分の独立評価を行い、PRでは同じheadの `checks`・`verify` を確認する。既存の関連テストを使い、不足する現実的な失敗条件だけ追加・更新する。プロダクト画面媒体は不要。

実装・テスト・文書の増減と、同じ範囲・粒度でのノード・矢印の増減を既存のPR説明に記す。実際の処理・操作の削減と表示上の集約を区別し、純減がなければその理由を示す。行の圧縮、単なる移動、新しい計測基盤・必須帳票は目的にしない。変更を挟む再照合、停止時の保全、独立評価、人の要求判断とマージを維持する。

確認日: 2026-09-19。調査版: `0fec56c536763f8ccbd4bc4a9080c19beb14862d`。依頼者が整理候補の提示を受けて「issue化してください」と指示した範囲。今回はIssue公開までとし、実装は後続の着手指示で進める。着手時に最新の採用版へ照合し直す。必要な根拠は本文と固定版の参照で足り、未共有の下書きや生ログを必須入力にしない。本Issue自体の別評価者による独立評価は未実施。
