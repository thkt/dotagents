## 目的と現状

通常の流れを見やすくしつつ、元の最小粒度のノード・矢印・停止理由をたどれる状態を保つ。ガイドの現行版と同内容の下書きを二重に更新する手間も減らす。

2026-09-17のローカル資料 `docs/workflow-map.html` は202ノード・318矢印、停止への矢印は96本。 `docs/workflow-guide.html` と `docs/workflow-guide-draft.html` は同じ内容だった。これらは現在Git未追跡のローカル資産であり、GitHub上に共有済みとは扱わない。

## 検討する最小案と完了条件

- [ ] 通常表示では、十分性の六つの問い（S16〜S21）と独立評価の四観点（R07〜R10）をそれぞれ一つの評価ノード内へまとめる案を具体化する。個別観点・根拠は詳細で読めるようにする。
- [ ] 停止への接続は、工程ごとの異常出口と停止理由から必要時に展開する案を比較する。特定ノードの失敗条件・停止理由・戻り先を追えることを受入条件にする。
- [ ] 全ノード・全矢印を確認する表示と既存のノードIDへの参照を維持する。元データや検査を削って表示数を減らさない。
- [ ] ガイドの編集元を一つに定め、下書きを変更中だけ扱う方法を示す。同一内容の旧資産も保全し、削除や無確認の上書きを前提にしない。
- [ ] commit・開始条件・上限など、ガイドと図で共有する事実の最小範囲を選ぶ。既存定義からの表示と手動編集を比較し、全文生成の基盤を先に作らない。
- [ ] ローカル編集元の所在・版と、後続実装者へ渡す方法・公開範囲を確定する。未追跡HTML全体の公開repoへの追加は、このIssue作成だけでは許可されたと扱わない。
- [ ] 通常／全詳細、検索、ノード選択、停止元と理由、狭い画面での操作を確認する方法と、必要な画面証拠の保存先を決める。

## 効果と次の判断

六つの問いと四観点をまとめるだけの静的試算は202→194ノード、318→310矢印。停止矢印の表示案を含まない試算であり、モデル呼び出し・実際の検査・処理回数の削減ではない。後続のコード変更を反映した版で再計算する。

根拠となる観点は [skills/scoping/references/sufficiency.md](https://github.com/thkt/dotagents/blob/019a25badbcec00a05231267b31fed453a49bf74/skills/scoping/references/sufficiency.md#L3) と [scripts/review.ts](https://github.com/thkt/dotagents/blob/019a25badbcec00a05231267b31fed453a49bf74/scripts/review.ts#L234)、開始条件は [docs/knowledge/implementation-start.json](https://github.com/thkt/dotagents/blob/019a25badbcec00a05231267b31fed453a49bf74/docs/knowledge/implementation-start.json) を参照する。

未決定なのは図の具体的な切替操作、編集元の共有範囲、過去版の保管方法である。本Issueは表示・編集方針と引き継ぎ条件を確定するまで。ローカル資産を本番変更したり、一括削除・公開したりしない。コード改修によるフロー変更の反映と、見た目の集約を分けて評価する。

## 関連Issue

比較・設計に利用する先行候補: [#100](https://github.com/thkt/dotagents/issues/100)、[#101](https://github.com/thkt/dotagents/issues/101)、[#103](https://github.com/thkt/dotagents/issues/103)。結論が変わる場合は最新の採用状況と照合する。

## 実施条件と合意

確認日: 2026-09-17。調査版: `019a25badbcec00a05231267b31fed453a49bf74`。依頼者は簡素化候補の提示を受け、同日に「issue化しましょう」と指示した。今回は本Issueの調査・設計をIssueとして残す。未決定の仕様を採用済みとは扱わず、本番の実装・公開・旧runの再開は含めない。

開始時は最新の採用版と関連方針を再確認する。現状維持と既存部品を使う最小案を比較し、減らす操作・処理と増える保守負担を示す。結論と根拠は本Issueへ集約し、長い共有根拠が必要な場合だけ `research/` へ置く。生ログ・ホスト内のパスは公開しない。

文書・コードを変更する場合のセットアップは `bun install --frozen-lockfile --ignore-scripts`、提出前の検証は引数なしの `bun run check`。読解だけの調査に不要なテスト追加・実モデル起動・公開試行を課さない。機能の採用に進む場合は、残る判断と具体的な実装範囲・受入条件を本Issueへ反映して合意する。独立評価・人の要求判断とレビュー・マージを維持する。別評価者による本Issueの独立評価と改善効果の実測は未実施。

## コードとフローの削減評価

2026-09-18、依頼者の追加指示を反映。従来の動作要件と、実装／調査・設計の区分を維持する。

- [ ] 追加の前に削除・一本化で解決する案を検討し、置き換えた旧処理・重複する指示・不要な受け渡しを残さない。テストも既存の検出力と重複する準備・ケースを整理する。
- [ ] 着手版と成果物を比べ、実装・テスト・文書の行数増減と、対象フローのノード・矢印の増減を既存のIssue／PRへ短く記す。実際の操作・判断・受け渡しと図の表示集約を分け、同じ対象範囲・粒度で比較する。調査Issueは案の比較までとし、推定と実測を区別する。
- [ ] 単なるファイル移動や行の圧縮を削減効果に数えない。削減なし・増加も明記し、増えるコードが減らす手順・分岐・受け渡しと保守負担を比較する。全項目の純減をノルマにせず、新しい測定基盤・必須帳票は作らない。
- [ ] 必要な異常検出、対象・権限の照合、独立評価、人の判断を維持する。検査やテストの統合・削除は、どの現実的な不具合を残る検証で検出するか説明する。変更を挟む再照合を単なる重複としない。

### このIssueで狙う削減

- 表示を畳むだけでなく、ガイド・下書きの全文コピー、重複する表示定義・共通事実の保守を減らす案も比較する。過去版は保全し、現在の編集対象を増やさない。共有の仕組み自体が大きくなる場合は最小の編集元一本化を優先する。
- 実際のフロー、元データの全詳細、通常表示のノード・矢印を分けて数える。202→194／318→310は評価観点の表示集約だけの試算として維持し、コード改修の成果へ合算しない。図の切替機能やレンダラーの増加分もコード量へ含める。

## 構造図・HTMLとコード全体の監査で補う点（2026-09-18）

依頼者から構造図・HTMLの不要なノード・接続と、コード全体の不要・重複・冗長をJevで確認する依頼を受けた。図は `019a25badbcec00a05231267b31fed453a49bf74` を参照する202ノード・318接続、コードは採用版 `7e1d69b550197fa343c0e4bd7246a6ab503585d0` を対象に照合した。以下は図の更新漏れと説明上の責任分担を補う観点であり、新しいruntime機能の追加ではない。

- [ ] 終端判定前に知識を読む `V04 → V16 → V05` の順序を現行コードに合わせる。[PR #107](https://github.com/thkt/dotagents/pull/107)採用後の[execute](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/correction.ts#L854)は、保存済み結果があれば必要なblob・Issue・sourceを照合して戻り、`readKnowledge` は未完了経路だけで呼ぶ。V16自体は未完了経路に必要なので、全経路から削除しない。
- [ ] `V06 → V13` の「同一・accepted」が、保存済みの成功結果を再保存する処理に見えないようにする。V13の名称は「ready_for_human_reviewを保存する」だが、現行の終端経路は[保存せず結果を返す](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/correction.ts#L864)。新規成功結果の保存と既存結果の返却を接続・説明で区別し、呼出元に応じた戻り先を示す。既存の停止結果を返す経路も残す。
- [ ] [PR #108](https://github.com/thkt/dotagents/pull/108)後の引き継ぎ責任を、R11・P06とガイドのPR生成説明へ反映する。[レビューAI](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/review.ts#L245)が返すIssue固有の残件と、[ホスト](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/pr-body.ts#L58)が追加する定型の公開・CI・人の確認を区別する。図やガイドから、定型残作業をAIが再生成する手順を復活させない。

構造データの機械確認では、接続先不明、完全に同じ接続、E01から到達不能なノードは各0件だった。ガイドと下書きは今回も同一内容。S16〜S21とR07〜R10の集約は194ノード・310接続という表示上の試算を再確認したが、六つ・四つの評価内容と異常検出は残す。図の古い接続の修正は、既にコードで省かれた処理を図からも除く対応であり、新たな実行回数削減として数えない。

Jevには全ノード・接続、HTML本文・埋込みスクリプト、テストを含むTS33ファイルを渡して候補を判定させた。上記の具体的な更新漏れは担当AIがコードと図を照合して確認したもので、Jevが全件を自律発見したとは扱わない。例えば知識読込みの順序は、局所的な事実確認と総合的な不整合判定で回答が揃わなかった。Jevの確率は削除許可や正しさの証明には使わない。

今回の反映は監査結果の追記まで。図・ガイド・コードの変更、ローカルHTML全体のGitHub公開、別評価者による独立評価は未実施。ブラウザのローカルURL制限により画面表示は未確認で、HTML内の構造データ・本文・スクリプトを直接読んだ。後続の表示変更では既存の画面確認条件を適用する。

## 2026-09-19 現行資産の確認と採用した最小修正範囲

コードの調査版は `0fec56c536763f8ccbd4bc4a9080c19beb14862d`。ローカルの図・現行ガイドは `a0ec660b4c5cf36f0f1a18f6ce3f015d8267c875`、旧下書きは `019a25badbcec00a05231267b31fed453a49bf74` を参照している。以下のHTMLは引き続きGit未追跡であり、GitHub上の実装済み資産とは扱わない。

既存機能と残る不整合、下記の最小修正範囲・受入条件を提示し、依頼者は同日に「ローカル限定の最小案を採用」と回答した。既存の表示を維持し、返却経路・#115の説明・編集元の明示を修正範囲とする。HTMLは従来どおりローカル限定、旧下書きは保全し、Issueには方針と受入条件を残す合意である。

### 既に満たしている点

- 図には「工程ごとに読む」と「接続図を見る」があり、読む表示ではS16〜S21・R07〜R10をそれぞれ一つの開閉枠へまとめている。元のID・説明・接続は共通の `flow-data` に残り、検索で個別ノードを選ぶと枠が開く。
- 停止条件は工程別の一覧で、発生元・条件・停止先から詳細へ進める。全ノード表示は205ノード・229矢印、停止矢印を展開すると205ノード・332矢印になる。103本の停止矢印を表示上畳んでおり、データや実際の検査は削除していない。
- 通常／全詳細切替、S18検索からの展開と根拠参照、R13の形式・対象ID不正からZ08、その先のZ01への接続を画面操作で確認した。幅390pxでは工程選択・検索・折りたたみ内への移動が使え、図の読む表示にページ全体の横はみ出しはなかった。全ノードの矢印表示は専用領域内のスクロールで扱う。
- 元データの接続先不明、完全重複の矢印、E01から到達不能なノードは各0件。読取時のブラウザーコンソールにerrorは確認されなかった。これは全操作・全説明の正しさの保証ではない。
- ガイドは507行、旧下書きは455行で、現在は同一内容ではない。現行ガイド／図から旧下書きへのリンクはなく、旧下書きの同期更新を続ける必要はない。
- 前回監査の知識読込み順序は `V04 → V05 → 未完了時だけV16` に修正済み。R11・P06も、Issue固有の引き継ぎはAI、定型の残作業はホストという分担を反映している。

### 採用した最小修正

1. **表示機能を追加せず、既存の表示を使う。** 新しい集約ノード、別の通常用データ、第三の表示モード、生成基盤は作らない。従来の202→194／318→310という試算を現在の削減成果に流用しない。
2. **終端成功の返却経路を実装に合わせる。** 現在の `V06 → V13` は、保存済み成功の返却を「ready_for_human_reviewを保存する」処理へつないでいる。[終端経路](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/correction.ts#L724)は対象を再照合して保存せず返す。[公開前の呼出元](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/development.ts#L523)へ戻る経路と、新規成功結果の保存を説明・接続で区別する。対象不一致、以前の停止結果、知識blob欠落の経路を維持し、成功から初回公開判断へ戻って再実行するように読めないことを確認する。
3. **#115の採用内容と参照版を同期する。** 実装・修正の `findings` と独立評価の `assessments.tests` にテストの保証・費用・整理理由を残し、既存PR説明へ渡す責務を既存ノード／ガイド本文へ反映する。新しい評価工程を描かない。変更に関係する根拠リンクの位置と内容を照合する。
4. **編集元を一つに決める。** ガイド本文の編集元は `docs/workflow-guide.html`、図のデータ・表示の編集元は `docs/workflow-map.html` とし、別々の用途として維持する。`workflow-guide-draft.html` は旧版のまま保全し、同期対象・通常の入口から除外する。旧ファイルの削除・移動・上書きは不要。確認前の案が必要な変更だけ別コピーで比較し、恒久的な全文コピーを増やさない。この扱いは現行ガイドの出典欄へ短く明記する。
5. **共有事実は既存の正本を使う。** 版・確認日は両HTMLを同じコード版へ照合する。開始条件は既存の共有知識、回数・時間上限・CI条件はコードとCLIガイドを根拠に必要な説明だけ残す。図の件数は既存のデータから算出する。共通metadataファイル、動的なGitHub取得、全文自動生成は加えない。二つのHTMLを同時に照合する作業は残るが、抽出器・テンプレート・生成物の保守を増やさない。

### 共有範囲と引き継ぎ

従来どおりのローカル運用を採用する。Issueには決定・受入条件とコードの根拠だけを公開し、未追跡HTMLや画面証拠を公開repoへ追加しない。共有範囲を変える場合は、下記2ファイルの内容を確認した上で、追加先・レビュー方法・媒体公開範囲を別途確定する。

後続担当は同じ作業環境の `docs/` から現行2ファイルを読み、下記SHA-256と一致する版を基点にする。違っていれば差分を読み直し、古いコピーで上書きしない。共有されたGit HEADだけでこの未追跡HTMLを取得できるとは扱わない。別環境へ渡す場合は、合意した宛先へこの2ファイルを明示的に引き継ぐ必要がある。

| ファイル | SHA-256 |
| --- | --- |
| `docs/workflow-map.html` | `49f1ffd0aa11c70077f851801b81f4e77a13c9a3333f58bc47c3035acc157572` |
| `docs/workflow-guide.html` | `a46ba0cee87a81a54961d9f3246943ae6c2eb21e0a8741d45817b940096b275e` |
| 保全する `docs/workflow-guide-draft.html` | `8fdb45610abbfd9500b049db815f7a0e567f2f228ea72663c6ec8f1a393b05c4` |

### 受入条件と削減の扱い

- 更新前の2ファイルをcheckout外の今回の作業領域へ保全する。画面証拠も同じ領域の `workflow-106/` に保存し、旧下書きの内容・hashが変わらないことを確認する。公開Issueにはホストの絶対パス・生ログを載せない。
- 初期表示、工程選択、全205ノード表示、全接続展開、ID検索と直接リンク、個別ノードの根拠・前後の接続を確認する。R13→Z08→Z01を含む停止元・理由・戻り先が追え、表示を戻すと停止条件を再び畳めることを確認する。修正した終端経路はコードの呼出元と照合する。
- 通常幅と390px幅で主要操作・説明が読めることを、図とガイドの実画面で確認する。HTMLのIDと内部参照、データのID・接続先・完全重複を機械確認し、見た目だけの成功にしない。実行時の媒体やモデル呼出しは不要。
- 表示機能の再実装を省くため、runtime・共通テストの増減は0を想定する。HTMLの説明・接続・参照更新は未実施で、行数増減は未計測。必要な接続の修正は欠落・誤記の訂正であり、実処理削減と扱わない。元データ、表示中の項目、実際のフローを分け、件数を減らすために正しい戻り先を省かない。
- ガイドと旧下書きの二重更新は既に止まっているため、今回の新たな削減として数えない。編集方針の明示で再発を防ぐ。二つのHTMLの用途と根拠を保ち、文書内容・版の整合を既存の確認でレビューする。

今回の要求整理ではHTML本体・ハーネスのコードを変更していない。別評価者による今回の調査の独立評価は未実施。要求・受入条件・共有範囲は上記で確定し、現行2ファイルの局所修正と更新後の確認へ引き継ぐ。ローカル修正の完了までは本Issueを開いたままとし、HTML公開やruntimeの変更へ範囲を広げない。

## 2026-09-19 ローカル修正の完了

採用した最小範囲を実施した。現行ガイドと構造図の参照版を `0fec56c536763f8ccbd4bc4a9080c19beb14862d` へ揃え、保存済み結果の返却と新規結果の保存を分離した。公開前の共通呼出しは `P02 → V01 … V06 → P03` として、初回の公開判断へ戻るように見える接続を修正した。#115のテスト評価は既存ノードと本文へ反映し、本文・図の編集元と旧下書きの保全方針を明記した。

### 確認結果

- 初期表示、工程選択、全ノード・全矢印の表示、停止矢印の集約、ID検索と折りたたみ内の展開、直接リンク、根拠・前後の接続を確認した。停止一覧から `R13 → Z08 → Z01` をたどれ、一覧を再び閉じられる。
- 図とガイドを通常幅と390px幅で確認した。読む表示のページ全体に横はみ出しはなく、主要操作と今回の説明を読めた。確認時のブラウザーコンソールerrorは0件。
- HTMLとデータのID、内部参照、根拠ファイル、接続先、完全重複、E01からの到達性を機械確認した。停止接続103本、描画スクリプト、旧下書きのSHA-256は変更なし。
- `bun run check` は303件成功・失敗0。これは共通ハーネスの模擬コマンドによる検証であり、ローカルHTMLの画面確認とは別に実施した。実モデル、対象プロダクト、PR公開の試行は実施していない。
- 別評価者がIssue・コード・今回の変更を照合し、修正必須の不整合や根拠のある冗長性は確認されなかった。画面確認で見つけた短縮commit表記の更新漏れも修正し、その差分を再評価した。

### 増減と保存

| 対象 | 変更前 → 変更後 | 評価 |
| --- | --- | --- |
| runtime・共通テスト・描画スクリプト | 変更なし | 新機能・新しい評価工程は追加していない |
| 元データ | 205ノード・332矢印 → 205ノード・333矢印 | 正しい呼出し・戻り先を補正した1本増。実処理の増減ではない |
| 停止矢印を畳んだ全ノード表示 | 205ノード・229矢印 → 205ノード・230矢印 | 停止103本はデータに保持 |
| `workflow-map.html` | 254行 → 254行（2行追加・2行削除） | 既存JSONと変更説明の更新 |
| `workflow-guide.html` | 507行 → 508行（37行追加・36行削除） | 根拠リンク・表記・本文の更新。純増1行 |

二重更新は既に止まっていたため、新たな削減として数えていない。今回は再発を防ぐ編集元の明示と説明・接続の訂正を完了した。恒久的な生成基盤や共通metadataファイルは追加していない。

変更前の2ファイル、確認記録、画面証拠は今回のローカル作業領域 `workflow-106/` に保存した。旧下書きはそのまま保持し、HTMLと画面証拠を公開repoへ追加していない。引き継ぐ現行版のSHA-256は次のとおり。

| ファイル | 完了時のSHA-256 |
| --- | --- |
| `docs/workflow-map.html` | `c2c3d62adc6dcbc27c4ed94c56bd25485770d727c5c595f037119b7c642eed9e` |
| `docs/workflow-guide.html` | `d0a511c1fd6e70603248d22372fd9c2a8999599ff12d070e5d93d8b7af221a66` |

合意済みのローカル修正と確認を終えたため、本Issueを完了とする。
