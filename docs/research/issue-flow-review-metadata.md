## 目的と現状

AIに既知の固定値を再生成させず、指摘の内容と根拠の評価へ出力を絞る。

[scripts/review.ts](https://github.com/thkt/dotagents/blob/019a25badbcec00a05231267b31fed453a49bf74/scripts/review.ts#L24) は新規指摘に `introducedIn` と `disposition` を要求し、[scripts/review.ts](https://github.com/thkt/dotagents/blob/019a25badbcec00a05231267b31fed453a49bf74/scripts/review.ts#L205) で現在のtargetIdとopenに一致するか確認している。

## 範囲と完了条件

- [ ] 新規指摘のモデル応答から `introducedIn` と `disposition` を外し、ホストが検証済みtargetIdとopenを付与する。固定値の回答と一致検査をホストの構築へ置き換える。
- [ ] 応答全体のtargetId照合、過去の全IDへの判断更新、欠落・重複・未知IDの拒否、過去本文の保持、必須open指摘による公開抑止を維持する。
- [ ] JSON Schema・応答型・検査・指示文を揃え、保存される完全な指摘記録に導入対象・状態を残す。
- [ ] 新形式は新規実行へ適用し、旧runを自動変換・再開しない。既存の形式識別と保存状態の検査に必要な差分を明示する。
- [ ] IDの接頭辞生成、汎用スキーマ基盤、新しいライブラリ導入は含めない。今回の二項目に関係する重複だけを整理する。

## 重点的な検証・順序

[#82](https://github.com/thkt/dotagents/issues/82)の後続改善。handoffの整理と同じreview.tsを触るため、その採用後を推奨。

`review.test.ts` とcorrectionの関連テストで、ホスト付与、過去指摘の更新、対象不一致、不正応答、公開抑止を確認する。固定値の再回答ミスを検出するテストは新しい責任分担へ置き換える。実モデルの形式追従は既存入力を使う範囲限定の試行で別に確認し、未実施ならその限界を明記する。

## 関連Issue

先行候補・関連する整理: [#96](https://github.com/thkt/dotagents/issues/96)。本文の推奨順に従い、採用状況と変更範囲を着手時に照合する。

## 検証と引き継ぎ

セットアップは `bun install --frozen-lockfile --ignore-scripts`、提出前は引数なしの `bun run check`。既存の関連テストを再利用し、不足する現実的な失敗条件だけを追加・更新する。独立評価と同じPR headの `checks`・`verify` を確認し、人がレビュー・マージを判断する。模擬コマンドの検証、実モデル、実際の公開は別の証拠として報告する。

プロダクト画面媒体は不要。必要な共有根拠はIssueまたは `research/`、内部ログは既存のcheckout外保存先へ置く。既存のrun・失敗記録・他作業の差分を削除・変換せず、新しい必須台帳を作らない。対象・権限・版の照合と、要求変更を人へ戻す責任を維持する。

## 根拠と合意

確認日: 2026-09-17。調査版: `019a25badbcec00a05231267b31fed453a49bf74`。依頼者はコードと手順の簡素化候補の提示を受け、同日に「issue化しましょう」と指示した。本Issueはその候補の範囲を定める。今回はIssue公開までとし、実装は後続の着手指示で進める。効果の実測と独立した別評価者による本Issueの評価は未実施。

実装開始時に最新の採用版と関連方針へ照合し直す。調査版を開始commitへ固定しない。必要な根拠は本文とコミット済み参照に含め、ローカルのレビュー報告やIssue下書きを必須入力にしない。

## コードとフローの削減評価

2026-09-18、依頼者の追加指示を反映。従来の動作要件と、実装／調査・設計の区分を維持する。

- [ ] 追加の前に削除・一本化で解決する案を検討し、置き換えた旧処理・重複する指示・不要な受け渡しを残さない。テストも既存の検出力と重複する準備・ケースを整理する。
- [ ] 着手版と成果物を比べ、実装・テスト・文書の行数増減と、対象フローのノード・矢印の増減を既存のIssue／PRへ短く記す。実際の操作・判断・受け渡しと図の表示集約を分け、同じ対象範囲・粒度で比較する。調査Issueは案の比較までとし、推定と実測を区別する。
- [ ] 単なるファイル移動や行の圧縮を削減効果に数えない。削減なし・増加も明記し、増えるコードが減らす手順・分岐・受け渡しと保守負担を比較する。全項目の純減をノルマにせず、新しい測定基盤・必須帳票は作らない。
- [ ] 必要な異常検出、対象・権限の照合、独立評価、人の判断を維持する。検査やテストの統合・削除は、どの現実的な不具合を残る検証で検出するか説明する。変更を挟む再照合を単なる重複としない。

### このIssueで狙う削減

- 新規指摘の二つの固定項目を、モデル用Schema・応答型・検査・指示・fixtureから整理する。保存用の完全なReviewとの共用が不要な検査を残す場合は、必要な境界だけ分ける。新しい汎用スキーマ基盤を追加して相殺しない。
- 図では「固定値をAIが再生成→ホストが一致検査」という受け渡しをホストの付与へ置き換える。targetId全体、過去ID、必須openの検査を残し、同じ評価内の細目削減と独立したノード削減を区別する。

## Jev試験から補う確認観点（2026-09-18）

- [ ] 生成用Schema、モデル応答の受信検証、保存済みの完全なReviewの検証を、同じ項目が現れるだけで重複と扱わない。モデルへの出力指定と、実際に受け取ったJSON・保存記録の異常を拒否する検査は役割が異なる。固定二項目の再回答要求と一致検査を除く際も、それぞれ必要な検査を残す。
- [ ] 総合statusと過去指摘の不変本文は既にホストが構築しているため、新たな削減成果に数えない。今回減らす対象は `newItems` の `introducedIn` と `disposition` に絞り、応答全体のtargetId照合と過去指摘の更新判断は維持する。

根拠は、採用版 `7e1d69b550197fa343c0e4bd7246a6ab503585d0` の[生成用Schema](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/review.ts#L19)、[応答・保存用の検証](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/review.ts#L142)、[過去指摘とstatusの構築](https://github.com/thkt/dotagents/blob/7e1d69b550197fa343c0e4bd7246a6ab503585d0/scripts/review.ts#L183)。

Jevの先行フロー試験（対象 `019a25badbcec00a05231267b31fed453a49bf74`、回答モデル `jev-1.13.0`）では、レビュー項目定義の共通化と過去指摘・statusの再出力について、参照範囲を絞ると分類が変わり、一部はコードの読解と一致しなかった。上記はJevの分類を採用判断にせず、担当AIが現行コードを再確認して補った観点である。Schema全体の共通化、汎用基盤、Jevの必須導入へ範囲を広げない。

依頼者の「jevの観点で漏れてるものがあればissueに追記してほしい」に基づく補足。今回の作業は既存試験と現行コードの照合・Issue更新までで、追加のJev実行と本Issueの実装は未実施。
