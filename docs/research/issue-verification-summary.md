## 位置付けと関連（2026-09-19整理）

優先度: 高。#123・#132・#133に続く候補。#132は保持する記録の保存障害と停止後の対応、本Issueは不要な派生ファイルの廃止に限定する。

停止状態と次の対応を既存のresult/stateから辿れるようにし、要約ファイル削除を理由に証拠・判断材料・再判定条件を減らさない。

## 目的と根拠

[development](https://github.com/thkt/dotagents/blob/0fec56c536763f8ccbd4bc4a9080c19beb14862d/scripts/development.ts#L477)は検証後の `state.findings` を `verification-summary.md` へ写す。一方で、終了案内の `details` は既に `verification/state.json` を指す。repo内で要約Markdownの本番処理による読取りは確認できず、保存処理、テスト、CLI説明に参照がある。repo外の独自利用者は未確認。

既存のstateと履歴を正本として、同じ内容のファイルを選ぶ手間と、受入済み評価の後に起きる追加の書込み失敗を減らす。#103の終了案内一本化を補うが、下位stateや生ログの統合へ広げない。

## 範囲と受入条件

- [ ] 新しいrunでは `verification-summary.md` の保存を廃止し、`result.json` の案内から検証stateの要約・評価履歴・生ログ参照を読める。
- [ ] 人向けのCLI説明を実際の読取り経路へ更新し、必要な要約内容を失わない。別の要約ファイル・新しいCLIは追加しない。
- [ ] 既存の `verification/state.json`、評価履歴、生ログ、過去run内の要約Markdownは保全する。
- [ ] 通常公開とno-publish、検証停止時に理由・次の対応・残作業を保持する。
- [ ] repo内の読取り側とテストを再確認し、廃止したファイルの存在だけを要求する検証を残さない。repo外の未確認利用を互換性確認済みとは説明しない。

主な検証は `development.test.ts` と検証stateの既存テスト。保存処理・派生ファイル・読取り先の選択を減らす。現行図で独立していない細目を削っても、ノード削減へ換算しない。

## 検証・削減評価・引き継ぎ

セットアップは `bun install --frozen-lockfile --ignore-scripts`。変更の提出前は `bun run check` と差分の独立評価を行い、PRでは同じheadの `checks`・`verify` を確認する。既存の関連テストを使い、不足する現実的な失敗条件だけ追加・更新する。プロダクト画面媒体は不要。

実装・テスト・文書の増減と、同じ範囲・粒度でのノード・矢印の増減を既存のPR説明に記す。実際の処理・操作の削減と表示上の集約を区別し、純減がなければその理由を示す。行の圧縮、単なる移動、新しい計測基盤・必須帳票は目的にしない。変更を挟む再照合、停止時の保全、独立評価、人の要求判断とマージを維持する。

確認日: 2026-09-19。調査版: `0fec56c536763f8ccbd4bc4a9080c19beb14862d`。依頼者が整理候補の提示を受けて「issue化してください」と指示した範囲。今回はIssue公開までとし、実装は後続の着手指示で進める。着手時に最新の採用版へ照合し直す。必要な根拠は本文と固定版の参照で足り、未共有の下書きや生ログを必須入力にしない。本Issue自体の別評価者による独立評価は未実施。
