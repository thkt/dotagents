## 結論・調査完了

**現状を維持し、直接返却は実装しない。** 独立評価からの未受入指摘は引き続き修正担当へ渡し、人にしか決められない不足が残れば既存のneeds_humanで返す。

2026-09-20、依頼者thktは、下記の比較結果と「人の判断待ちが必須なら修正可能な指摘が混在してもrunを停止する」「該当経路の修正AI呼出しを1回省ける」「図全体の矢印は1本増え、コードの純減は見込んでいない」を確認する質問に対し、**「現状維持で調査完了にする」**を選択した。この回答を採用・見送りの合意として記録する。

目的は、人にしか決められない要求・範囲・権限の不足が確認されたとき、判断材料を渡すためだけの修正AI呼出しを省けるか判断することだった。現行経路、直接返却の最小契約、代表例、削減効果と追加の保守負担を比較し、採用判断まで完了した。本Issueは調査完了として閉じ、実装待ちにはしない。

## 現行の根拠と今回の確認

確認日: 2026-09-20。対象: dotagents `f39f3caa2cab7f8f083d0577917345cae0929792`。

- [review.ts](https://github.com/thkt/dotagents/blob/f39f3caa2cab7f8f083d0577917345cae0929792/scripts/review.ts#L184)は、初回指摘にホストがID・初出対象・openを付け、再評価ではdispositionとreasonだけを更新する。必須かつopenの指摘が残ればneeds_changesになる。
- [evaluate / cycle](https://github.com/thkt/dotagents/blob/f39f3caa2cab7f8f083d0577917345cae0929792/scripts/correction.ts#L551)は、対象・形式・過去指摘・参照文書を照合し、評価保存後に進み先を選ぶ。未受入の評価は修正へ渡り、修正応答のneeds_humanでhuman_decision_requiredになる。
- [終了案内](https://github.com/thkt/dotagents/blob/f39f3caa2cab7f8f083d0577917345cae0929792/scripts/development.ts#L509)は判断材料を修正findingsと説明している。[PR本文](https://github.com/thkt/dotagents/blob/f39f3caa2cab7f8f083d0577917345cae0929792/scripts/pr-body.ts#L47)はopenな任意指摘のactionを表示する。直接返却案では両方の説明・扱いを揃える。
- #126のホスト採番と過去指摘の本文保持、#130の保存状態検査は採用済み。#155により通常の修正・独立評価は回数で打ち切られない。これらを迂回せず、人の判断が必要と確認できた時点の中継だけを減らす。

採用版と、その隔離コピーへ最小の制御変更を施した候補を、模擬コマンドで比較した。本番checkout・旧runは変更していない。

| 模擬応答の条件 | 現行 | 試作候補 |
| --- | --- | --- |
| 同じ対象を保持・削除するという相反した必須要求 | 評価1回・修正1回後に人へ返す | 評価1回・修正0回で人へ返す |
| 上記の判断依頼と、修正可能な文書不足が混在 | 評価1回・修正1回後に人へ返す | 評価1回・修正0回で停止し、両指摘を保持 |
| 合意範囲内で修正できる不足 | 比較対象外 | 修正1回・評価2回で受入 |
| 非必須の人への相談だけ | 比較対象外 | 修正0回・評価1回で受入、指摘は保持 |
| 未知の更新ID、評価中の対象変更、評価保存先の衝突 | 比較対象外 | invalid_review / source_changed / review_storage_failedで停止。修正0回、当該評価を履歴に追加しない |

直接の応答検査でも、初回の解決済み指定を拒否し、再評価でのopen・人の判断待ち・fixed・not_applicableの更新、IDと過去記録の保持を確認した。候補は問いを含む現在のreasonと評価記録への参照を既存findingsへ残す。

これは固定応答による制御の比較であり、実モデルが人の判断と調査不足を区別できる証拠ではない。不要な中継の実運用での頻度、誤った返却の頻度、時間・使用量・品質の改善は未測定。候補は本実装、共通check、独立評価、日本語確認を終えた成果物ではない。

## 比較した最小案と保守負担

直接返却案は、既存の指摘のdispositionにneeds_humanを加え、必須の人の判断待ちが残るとき、評価の照合・保存後に既存human_decision_requiredで停止するもの。初回のnewItemsはopenかneeds_humanに限定し、更新は既存のid・disposition・reasonを使う。問い・選択理由・影響・参照は現在のreasonへ記し、別のフラグ、質問配列、総合status、停止理由、CLI、モデル、保存ファイルを追加しない最小案を比較した。

| 観点 | 現状維持 | 直接返却の候補 |
| --- | --- | --- |
| 人への判断依頼の中継 | 修正AIを1回経由する。追加調査で人に尋ねず解消できる余地がある | 確認済みの判断依頼では修正AIを省ける。調査不足を人の判断と誤認する懸念がある |
| 指摘が混在する場合 | 修正担当が合意範囲で調べ、必要なら人へ返す | 最小案は当該runを停止し、修正可能な指摘も未解決で返す。独立作業の自動分割は加えない |
| 応答・保存 | 現行契約を維持する | 初回dispositionが新たに必須。独自reviewコマンド、入力検査、未解決判定、公開説明、関連テストの更新が必要 |
| 保存記録 | 現行記録を使う | 試作では指摘の項目とreviewFormat: 4を再利用できた。ただし新しい値と分岐の保守は残る |

代表例では、同じデータの保持と削除が同時に必須なら、関連資料・合意を読んでも矛盾が解消しないときに人の選択が必要となる。一方、関数の挙動や参照資料を調べていないだけなら担当AIが調査し、合意した境界値処理の誤りや範囲内の技術判断なら修正へ進める。候補の形式検査だけでは、この意味の区別を保証できない。

## コード・手順の増減

- 該当ケースの修正AI呼出しは1回省ける。専用の振り分けAI・評価工程・質問台帳は追加不要だった。
- 現行ローカル詳細図の判定R17から人の判断Z03までの経路は、R17 → X01 → X02 → X03 → X05 → Z03。候補の直接接続なら、該当経路は6ノード・5矢印から2ノード・1矢印になる見込みだった。
- 通常の修正経路は引き続き必要なため、候補でも図全体のノードは削除できない。既存の判定ノードから直接返す最小表現では211ノードのまま、359→360矢印となる見込み。全体のノード・矢印の純減ではない。
- 制御の試作はreview・correction・development・pr-bodyの4ファイルで16行追加・10行削除、純増6行。prompt、既存fixture・テストの更新、操作説明は未反映であり、完成形の差分ではない。コード純減の見通しは立っていない。
- 実運用での中継頻度と負担は未測定であり、追加する契約・分岐・保守に見合う効果を一般化できない。この比較を提示したうえで、依頼者は現状維持を選んだ。

## 再検討する条件と終了状態

実際のrunで、修正担当が追加調査や修正をせず、同じ判断依頼を伝えるだけのケースが繰り返され、その時間・使用量・失敗の負担を既存記録から具体的に示せた場合に再検討する。同時に、調査で解消できる不足を人へ返さない条件と、追加する契約・テスト・説明の保守負担を比較する。専用の計測基盤や台帳は、この調査の残作業にしない。

- [x] 現行の呼出経路、残す保証、最小の応答契約、関連する採番・保存検査との関係を確認した。
- [x] 代表例と隔離した制御試作で、直接停止・通常修正・混在・任意指摘・失敗時の扱いを比較した。
- [x] コード・手順の削減と追加負担、実測と未測定を区別して提示した。
- [x] 依頼者が現状維持を選択し、実装せず調査完了とした。

本番コード・スキル・テスト・採用済みHTMLは変更していない。隔離試作は未採用で、旧runの変換・再開・書換え・削除も行っていない。担当者が現行コード・試作結果・本文の事実、数量、条件、合意、参照を照合した。別評価者による今回の要求案の独立評価、実モデル試行、共通check、CIは未実施。調査とIssue整理だけのためにテストは追加していない。

比較の根拠・対象版・合意・再検討条件は本Issueへ集約した。一時試作や生ログを後続作業の必須入力にせず、新しい必須報告・再開台帳は作らない。後日再開する場合は、その時点のコード・実運用の証拠と新たな採用合意から判断する。
