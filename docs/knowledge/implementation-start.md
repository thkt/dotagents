# 実装開始の共有知識

人向けの生成説明は[wiki](../wiki/implementation-start.md)へ移動しました。

正本は引き続き[implementation-start.json](implementation-start.json)です。変更後は`bun run knowledge:generate`でwikiを生成し、`bun run knowledge:check`で一致を確認してください。旧Issueと保存済みrunのpath・blob・node IDは当時の版を読み、最新の説明へ自動置換しません。
